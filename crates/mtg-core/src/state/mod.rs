//! Game state: `GameState`, `Player`, the seven zones, and `Object`s.
//! CR 108–112 (objects/permanents/tokens/spells/abilities), CR 400 (zones).
//!
//! Stub — milestone 1+. See `docs/plans/ENGINE_PLAN.md` §4. State must stay cheaply
//! cloneable/serializable (index/`ObjId`-keyed arenas, not pointer graphs) for MCTS
//! and vectorized envs.
