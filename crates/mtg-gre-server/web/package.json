{
  "name": "mtg-gre-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "mtgenv web play client — TS/Vite front end over the JSON projection of the Agent boundary.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
